export interface FormValues {
  name: string;
  email: string;
  message: string;
}
export interface Project {
  title: string;
  description: string;
  skills: string;
  backgroundImg: string;
  githubUrl: string;
  siteUrl: string;
}
